fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
supabase==2.9.0
pydantic==2.12.4
python-multipart==0.0.20
pytest==8.3.4
httpx==0.27.2
ruff==0.16.3
PyMuPDF==1.28.2
python-pptx==1.0.2
groq==1.6.0
# --- Image understanding (v0.3.5) ---
# torch on Windows installs CPU-only from PyPI by default.
torch==2.13.0
open_clip_torch==3.3.0
imagehash==4.3.2
pillow==12.3.0
# v0.3.6: the optional Gemini vision describer intentionally adds NO
# dependency — it calls Google's REST endpoint via the already-pinned
# httpx, because the official google-genai SDK requires httpx>=0.28
# which conflicts with supabase==2.9.0's httpx<0.28 constraint.
