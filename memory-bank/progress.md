# Progress

<!-- Running log. Append, don't rewrite history. Newest at top.
     This tracks WORK STATUS (agent-facing, session to session).
     For the forward-looking feature plan, see ROADMAP.md instead —
     don't duplicate the full plan here, just what's actually in motion. -->

## Done

- v2.3.0 Explainability implemented (branch `feature/v2.3.0-explainability`) — Added `cited_excerpt` field to `CriterionScore` dataclass and all four specialist scoring agents. Agents now require a direct quote or section reference in their JSON response. Migration `0010_add_score_citations.sql` adds column to `scores` table. Updated `insert_scores` and `get_all_scores` in supabase service. Updated scoring route response. Updated `ScoreRow` type and `SubmissionDetailView` to display citations as blockquotes. Complete design system overhaul: 396-line CSS with custom properties (colors, typography, spacing, shadows, radius), dark mode support, responsive layout, app shell with sticky header nav, component classes (cards, buttons, badges, tables, score bars, rank badges, forms, alerts, citations). 104 tests pass (32 backend + 72 agents), ruff clean. — 2026-09-01
- v1.2.0 LIVE-VERIFIED by user (2026-08-26) — migrations 0008+0009 applied, SMTP credentials configured, real send-flow exercised: upload confirmation received; batch score → batch feedback run; results emails arrived with EXPLICIT outcomes (shortlist congratulations AND the new "not shortlisted" copy). Release train approved: merge `feature/v1.2.0-notifications` ← `feature/v1.2.0-batch-feedback` ← `fix/results-email-explicit-outcome` fast-forward to main, tag `v1.2.0`, push. — 2026-08-26
- Results-email outcome fix — non-shortlisted teams now get an EXPLICIT verdict: headline "Result: your submission was not shortlisted." plus a softening explanation sentence, in BOTH text and HTML bodies (shortlisted copy unchanged). Root cause: `_results_content()`'s else-branch used an outcome-free headline ("Your submission has been fully evaluated.") while only the shortlist branch named the result; the old tone test asserted the vague WORDING ("fully evaluated") instead of the OUTCOME, so it pinned the bug. Regression test rewritten as an outcome-property test (`test_results_email_states_outcome_explicitly`): rejected text+HTML must contain "not shortlisted" and must NOT contain "made the shortlist", shortlisted vice-versa. Verified red→green (probe before/after), full suite pytest **392**, ruff clean. Commit `fix/results-email-explicit-outcome` branch. LESSON (user-called-out): tests that assert implementation strings rather than user-visible properties let copy bugs through — write content assertions as properties. — 2026-08-26

- Batch feedback generation implemented — branch `feature/v1.2.0-batch-feedback` (stacked on `feature/v1.2.0-notifications`). Backend: `supabase.get_all_feedback_ids()` (one minimal-column query); `routes/feedback.py` refactored so `generate_submission_feedback` delegates to NEW shared `_generate_one_feedback(sid, hackathon_id, top_n)` (exact `_score_one_submission` pattern — single vs batch can never diverge); NEW `POST /api/submissions/feedback-pending?limit=&hackathon_id=&top_n=` — pending = ranked entries (leaderboard already excludes unscored/partial) not in the feedback-id set, processed BEST-COMPOSITE-FIRST one at a time (free-tier RPM), per-item HTTPException caught into `results[]`, envelope `{generated, failed, remaining, results[{submission_id, team_name, ok, verdict|error}]}`, `remaining = max(len(pending)-len(batch),0)` (score-pending semantics); Supabase-down at preload → whole-run 503; each success emails that team's results via the existing hook. Frontend: `generatePendingFeedback(limit,{hackathonId,topN})` in lib/api.ts; dashboard "Generate all pending feedback" card (own limit input, busy state, Generated/failed/remaining summary + per-failure lines, board reload) forwarding the APPLIED top-N cutoff. Tests: new `backend/tests/test_batch_feedback.py` (+13: missing-only generation, unscored/partial invisibility, ordering, limit/remaining, GroqError isolation, empty pool, all-done pool, 503, 422 limit/top_n, single-endpoint regression post-refactor) + dashboard/api Vitest cases; suites pytest **392** / Vitest **45**, ruff + ESLint clean. — 2026-08-26
- v1.2.0 Notifications implemented (code-complete) — branch `feature/v1.2.0-notifications`. Backend: migration `0009_add_team_email.sql` (nullable `team_email` + format CHECK via DO block); `insert_submission(team_email=...)` stores blank as NULL; NEW seam `backend/services/email.py` — `EmailResult(sent|skipped|failed, reason, detail)` RETURNED never raised, so mail can never fail upload/feedback; `EMAIL_PROVIDER=smtp|resend` (default smtp): SMTP = stdlib smtplib STARTTLS + App Password (zero deps), Resend = thin REST over pinned httpx, NO SDK (needs httpx>=0.28 vs supabase pin — ADR-0003 trap), retries only 429/transport ×3 backoff through `_sleep`; CR/LF header sanitization + html.escape'd multipart bodies; config.py email_* settings + `is_email_configured` + `frontend_urls` (main.py CORS origins now from Settings). Routes: POST /submissions optional `team_email` Form field (malformed → 400 pre-persist; blank passes "" to service which skips), confirmation fires via asyncio.to_thread AFTER parse-complete, response carries `notification.confirmation_email{status,reason}`; POST feedback sends results after upsert (tone follows shortlist; regeneration deliberately re-emails), response carries `notification.results_email`. Frontend: portal Contact-email input (inline validation, formReady gate, type=text per jsdom lesson), `uploadSubmission(teamName, teamEmail, file, opts)`, success message reflects sent/skipped. Tests: +23 backend incl. new test_email_service.py (dispatch routing, unconfigured/unknown-provider skips, injection-recipient rejection, SMTP starttls→login→send + auth-fail mapping, resend payload/auth/429-backoff/terminal-401/transport-retry, escaping, tone, link) and adversarial route tests proving NO mail on any 4xx/5xx path; Vitest updated signatures + invalid-email-blocks-submit. Suites: pytest **379** / Vitest **41**; ruff + ESLint clean; Prettier EOL churn reverted per policy; ALL route-exercising test files autouse-mock the mailer (live .env creds safety). Docs: ADR-0004, setup.md §5.4, .env.example block; version synced 1.2.0 everywhere. OUTSTANDING: user applies migration 0009 + real creds → live send verification; changelog regen at commit. — 2026-08-26
- CSV/PDF shortlist cutoff fix — Dashboard "Export CSV" downloaded a board where EVERY team showed "no" even though top 3 appeared shortlisted on screen. Root cause: the ranking engine shortlists NOTHING when no cutoff is given, and GET /api/export/csv already forwards top_n/min_score, but the dashboard built its Export CSV link from `exportCsvUrl(hackathon_id)` alone — the appliedTopN=3 that produced the on-screen view never reached the URL. Fix: `exportCsvUrl(hackathonId, {topN,minScore})` and `exportPdfUrl(submissionId, {hackathonId,topN})` now accept cutoffs and the dashboard/detail links forward the live `appliedTopN`/`topNInput` context (also closed the same latent gap in the per-team PDF, which previously fell back to the server default top_n=5). Committed 2026-08-25 on `feature/v1.1.0-submission-ux` as f4885e6. Regression coverage: frontend URL-builder tests (CSV top_n/min_score, PDF hackathon_id/top_n), dashboard test asserting the Export anchor carries top_n=3 after Apply, and backend test `test_csv_without_cutoff_shortlists_nothing` pinning the no-cutoff ⇒ all-"no" contract behind the bug. Suites: pytest 346 / Vitest 40 pass; ruff check+format, ESLint, Prettier all clean. — 2026-08-25
- v1.1.0 Submission UX implemented — branch `feature/v1.1.0-submission-ux`, commits **9c47180** (frontend) + **b1db34e** (backend+migration). Delivered: (backend) `POST /api/submissions` gains `replace_existing` form flag — duplicate team upload → 409 with human detail unless set; `supabase.insert_submission(supersedes_team=True)` stamps `superseded_at` on prior actives BEFORE inserting; new `get_active_submission_by_team()` (ilike + Python exact-match guard vs %/_ wildcards); `list_submissions(include_superseded=False)` default so rankings/CSV/PDF/feedback/batch see actives only; migration `0008_allow_resubmission.sql` (column + partial index on lower(team_name)). (frontend) `uploadSubmission()` in lib/api.ts (multipart, ApiError-mapped); portal pre-submit validation (type/size/empty inline on file change, blank-name check, `formReady` disables submit, native `required` removed); 409 → conflict box with "Replace previous submission" retry; success message lists parsed section titles; new `StageTracker` (derived Submitted/Parsed/Scored/verdict stepper) + 10 s polling on detail view until verdict. Verification: pytest **345 passed** (test_submissions +3 resubmission cases, test_supabase_service +7 service-layer cases, fixtures updated in core_loop/image_understanding), Vitest **36 passed** (api +4 uploadSubmission, page rewritten 6 cases incl. oversized-inline & replace-flow, detail +3 tracker/polling), ruff check+format clean, ESLint clean, Prettier canonical. Root-caused during TDD: jsdom's constraint validation silently swallows submits when a `required` file input receives programmatic files — fixed by dropping `required` (inline messages own the UX anyway). OUTSTANDING: apply migration 0008 in Supabase SQL Editor (backend 500s against live DB without it), user review, merge/tag/push. — 2026-08-25
- v1.0.0 RELEASE wrap-up — branch `feature/v1.0.0-mvp-dashboard` fast-forward-merged to `main` and tagged **v1.0.0** (push pending explicit user approval). Release gates: `npm run lint` ✔ no warnings/errors (only the `next lint` deprecation notice — CLI migration is part of the deferred Next 16 upgrade); `npm run format` canonicalized 8 files of wrapping/EOF drift (style commit fff077a; remaining touched files were CRLF-only normalization absorbed by autocrlf); version synced 0.7.0→1.0.0 across version.py + frontend/package.json + both package-lock root fields + README feature list split into proper v0.7.0/v1.0.0 entries (chore commit 36088f0, drift-guard test green); full suites re-run on the final tree: pytest **335 passed**, Vitest **26/26**. LIVE read-only smoke via TestClient (no socket server needed — ASGI app talks straight to real Supabase through the loaded .env): /health ok; GET /api/rankings 200 with `rubric_source: configured`, 9 ranked, 0 unscored; GET /api/export/csv 200 with attachment headers; GET /api/submissions 200 (9 rows); GET /api/submissions/{id}/feedback 200 WITH stored feedback → migration 0007 verified against production data; zero Groq calls spent. OBSERVATION: live 'default' rubric holds extreme weights (problem_fit 0.10 / technical_depth 0.00 / feasibility 0.90 / innovation 0.00) — reset to equal 25% via the now-working dashboard editor if undesired. CHANGELOG regenerated from tag history via scripts/regenerate_changelog.py (never hand-edited). — 2026-08-25
- CORS PUT fix — Dashboard "Save rubric" failed with "Network error — could not reach the backend" while the server logged `OPTIONS /api/rubrics/default HTTP/1.1" 400 Bad Request`. Root cause: CORSMiddleware whitelisted only GET/POST/OPTIONS, but v0.6.0's `PUT /api/rubrics/{hackathon_id}` (the API's only PUT) sends a JSON body, forcing a browser preflight that Starlette rejects with 400 "Disallowed CORS method" for non-whitelisted methods. Fix: `"PUT"` added to `allow_methods` in `backend/main.py`; new regression test `test_cors_preflight_allows_put_for_rubrics` in `backend/tests/test_submissions.py` (asserts preflight 200 + ACAO origin + PUT advertised; reproduces the 400 on pre-fix code). Full suite green: 335 passed; ruff check+format clean. — 2026-08-25

- v0.7.0 release wrap-up - branch `feature/v0.7.0-feedback-export` fast-forward-merged to `main` (14e677c -> 7dbf476); tagged **v0.7.0** @ 7dbf476; CHANGELOG regenerated into dated [v0.7.0] section via regenerate_changelog.py; ROADMAP/memory-bank updated; pushed to origin with tag. FeedbackAgent + export delivered: `agents/feedback/agent.py` FeedbackAgent (Groq gpt-oss-120b; verdict constrained to {shortlist,reject}; tone follows the shortlist flag; scoring-specialist idioms: `_get_async_groq_client`/`_sleep` seams, MAX_RETRIES=3 corrective re-prompt loop, bool-guard on suggestion, dash normalization); migration `0007_create_feedback.sql` (one CURRENT row per submission via unique(submission_id), jsonb strengths/weaknesses, verdict CHECK, RLS per 0003/0006 pattern); service fns `upsert_feedback(on_conflict=submission_id)`/`get_feedback(limit(1)+None)`; routes `POST|GET /api/submissions/{id}/feedback` (404 unknown / 409 incomplete score set / 503 config-or-key) plus `GET /api/export/csv` (stdlib) and `GET /api/export/submissions/{id}/pdf` (`backend/services/pdf.py`, ReportLab pinned 5.0.1) - all four reuse the refactored `load_leaderboard()` so composite/rank/shortlist never diverge; Content-Disposition stems sanitized against header injection. Version 0.6.0->0.7.0 synced everywhere incl. stale package-lock root fields. Tests: backend 92 / agents 230; ruff check+format clean; uvicorn boot smoke test confirmed all new routes + clean 503 when unconfigured. CAVEAT: live credential END-TO-END verification was not possible in this offline workspace (no .env) - apply migration 0007 and smoke-test feedback/export with real credentials before full confidence. - 2026-08-25
- v0.6.0 release wrap-up — branch `feature/v0.6.0-weighted-ranking` fast-forward-merged to `main` (b418060 -> 1165476); tagged **v0.6.0** @ 1165476. LIVE E2E verified pre-merge on port 8011 with real Supabase+Groq: rubric GET probe returned 200 with the migration-0006 seeded equal weights (migration was already applied); PUT innovation-heavy rubric (15/20/25/40) persisted and rankings reflected it; uploaded+scored two live decks (8 Groq calls, no 429s): GuardianTrack "Deck A" Practical pf7/td3/fe9/inno3 and QuantumForge "Deck B" Moonshot pf2/td2/fe2/inno8; composites EXACTLY equaled sum(score*weight) under the configured rubric (Practical 5.1, Moonshot 4.4) with correct relative order on the populated 8-row board (ranks 4 and 6); top_n=2 returned exactly 2 shortlisted (ranks 1-2). Full-board requests with top_n=100 shortlist everything by design. CHANGELOG regenerated into dated [v0.6.0] section via regenerate_changelog.py; ROADMAP/memory-bank updated; pushed to origin with tag. NOTE: live 'default' rubric still holds the test weights (0.15/0.20/0.25/0.40). Test decks left in Supabase: "v06 DeckA Practical" id 8e19dbaa-1569-403d-be4d-6ea1e43658e4, "v06 DeckB Moonshot" id fa465899-f340-47f7-bbb5-62953246c23c (safe to delete later). — 2026-08-24
- v0.6.0 — Weighted scoring + ranking implemented on `feature/v0.6.0-weighted-ranking`: migration 0006 (`rubric_config` hackathon-scoped, CHECK-constrained weights, seeded equal 25% rows, RLS per v0.x pattern); pure engine `agents/ranking/engine.py` (DEFAULT_WEIGHTS fallback, validate_weights with fraction-or-percent normalization + bool/non-finite guards, on-the-fly composite rounded to 4dp so ties are bit-identical, deterministic sort composite DESC → innovation DESC → id ASC, tie groups flagged, top-N/min-score shortlists with inclusive threshold); service fns get_rubric/upsert_rubric(on_conflict)/get_all_scores; routes `GET /api/rankings` (mutually-exclusive cutoffs → 422, partial-rubric → equal-weight fallback flagged `rubric_source: "fallback"`) and `GET|PUT /api/rubrics/{hackathon_id}`. Version 0.6.0 synced everywhere (drift-guarded). Tests: backend 58 / agents 195 / frontend 3; ruff clean. NOT merged/tagged/pushed; migration 0006 must be applied by user before live verification. — 2026-08-24
- v0.5.0 release wrap-up — branch `feature/v0.5.0-multi-agent-split` fast-forward-merged to `main` (7f0c66b -> 2874b72); tagged **v0.5.0** @ 2874b72. LIVE E2E verified pre-merge on port 8010 with real Supabase+Groq: upload 201 (2.6s incl. parse) -> POST score 200 in **4.76s** (four concurrent gpt-oss-120b calls, no 429s) -> GET detail readback: 4 rows, all agent_version=v0.5.0; cross-agent disagreement confirmed live (problem_fit 7, technical_depth 4, feasibility 9, innovation 3 — sensible spread for an off-the-shelf IoT proposal). CHANGELOG regenerated into dated [v0.5.0] section via regenerate_changelog.py; ROADMAP/memory-bank updated; pushed to origin with tag. Test submission left in Supabase for inspection: team "v0.5.0 Live Test" id 44f22d4e-48bc-4754-87bf-1db7fd002483 (safe to delete later). — 2026-08-24
- v0.5.0 — Multi-agent split implemented on `feature/v0.5.0-multi-agent-split` (e216eb4 feat + c57c61c version bump): `agents/scoring/base.py` adds the shared `SpecialistAgent` (AsyncGroq client seam, retry/backoff via an `_sleep` indirection, strict single-criterion JSON validation incl. explicit bool-score rejection) plus result types and the rubric single-source; four narrow agent modules carry only criterion + focused guidance prompts (technical_depth explicitly document-only: repo links never evaluated); `scorer.py` rewritten as orchestrator — asyncio.gather(return_exceptions=True), raise-first-failure = fail-closed, results re-ordered canonically by CRITERIA_NAMES — while keeping a stable facade (CriterionScore/ScoringResult/AGENT_VERSION/RUBRIC/build_scoring_text re-exported). Route awaits async score_submission; response shape unchanged; no DB migration (scores.criterion identifies the agent 1:1 beside agent_version). Backend test fakes for the scorer became async. New adversarial suites: per-agent validation matrix (wrong-criterion echo, boolean/"seven"/0/11 scores, non-dict roots, blank justifications, corrective-reprompt inspection), rate-limit/connection retry semantics, concurrency-overlap proof (max_active>=2, wall-clock <75% of sequential), PPTX-only end-to-end through real extractor fixture, image-description-merge regression. Tests: backend 38 / agents 166 / frontend 3; ruff check+format clean. NOT merged/tagged/pushed; live verification pending. — 2026-08-24
- v0.4.0 release wrap-up — branches fix/version-single-source, chore/changelog-versioned-sections, feature/v0.4.0-core-loop-checkpoint fast-forward-merged to `main` (8cf2527 -> b25a7d1); tagged **v0.4.0** @ b25a7d1; CHANGELOG regenerated post-tag (v0.4.0 section: version-centralization fix + read-API feature) and committed as 317211a; pushed to origin with tags incl. backfilled **v0.1.0**/**v0.2.0**. Post-merge sanity: backend 38 pass. — 2026-08-24
- v0.4.0 checkpoint — core loop demoable end to end, LIVE-verified: added read API (GET /api/submissions newest-first; GET /api/submissions/{id} composing submission+parsed+scores, orphan-tolerant, malformed-UUID 404 pre-Postgres), service fns list_submissions/get_submission/get_scores (.limit(1)+None pattern), mocked-loop integration test test_core_loop.py + route tests test_read_routes.py, README curl demo section. Live run on port 8010 with real Supabase+Groq: upload 201 -> parse 92 chars -> score 200 agent_version **v0.4.0** persisted per row -> detail+list readback OK; deliberately-sparse proposal scored 2-4/10 with substantive justifications. Branch feature/v0.4.0-core-loop-checkpoint commit ed72af1 (stacked). Tests: backend 38 / agents 106 / frontend 3 pass; ruff clean. — 2026-08-24
- Version consistency fix — root version.py APP_VERSION single source (0.4.0); FastAPI metadata (was hardcoded 0.1.0), scorer AGENT_VERSION (was frozen v0.3.0 while logic changed in v0.3.x), frontend/package.json (+lock), README status all derive/synced now; DESCRIBER_VERSIONs intentionally stay per-module provenance. Drift-guard suite backend/tests/test_version.py; de-hardcoded v0.3.0 assertion in test_scoring.py. Convention documented in systemPatterns.md. Branch fix/version-single-source commits 7a7e3b5+ab9ca91. — 2026-08-24
- Changelog restructure — regenerate_changelog.py rewritten from range-splice into [Unreleased] to FULL-history rebuild: git-cliff over all commits -> dated per-version sections (newest first, [vX.Y.Z], Bug Fixes before Features inside, Unreleased only when post-tag commits exist), preamble preserved, sha links kept. Added local lightweight tags v0.1.0@c8ca8be / v0.2.0@ea558e7 so early history groups correctly (push with next release). CHANGELOG regenerated accordingly. Also fixed pre-existing ruff format violation in old script. Branch chore/changelog-versioned-sections commit 9429a5c. — 2026-08-24

- v0.3.6 release wrap-up — branches `feature/v0.3.6-gemini-vision-provider`, `fix/v0.3.6-gemini-model-404`, and `fix/vision-classification-routing` fast-forward-merged to `main`; tagged **v0.3.5** @ `937a78f` (image-understanding milestone; its pending post-reset verification was satisfied by the successful live scango.pdf runs this session) and **v0.3.6** @ merged HEAD; pushed to origin with tags. CHANGELOG regenerated via git-cliff (`scripts/regenerate_changelog.py`). — 2026-08-24
- v0.3.6 fix — CLIP misclassification + wasteful decoration describes: live scango.pdf probes showed the process-flow diagram topping out as "logo or icon" 0.63 (flowchart 0.26) while banners/headers sat at 0.58–0.64 — all sub-threshold, so all were vision-described. Root causes: (1) routing described EVERY sub-threshold image including obvious decoration; (2) OpenAI CLIP weights loaded without QuickGELU (open_clip mismatch warning) degrading calibration. Fixes: classify_image relabels a decorative top to the best diagram label when it clears `IMAGE_DIAGRAM_FLOOR` (new env, default 0.30, calibrated on live probes: banners ≤0.27, misread flow 0.32); pipeline drops decorative-labeled images at ANY confidence; `force_quick_gelu=True` for openai weights. Live-verified end-to-end: exactly 3 describes (flowchart/chart-or-graph/architecture-diagram) + 11 silent drops, QuickGELU warning gone. Tests: 133 pass (+6), ruff clean. Commit `9065e11` on `fix/vision-classification-routing`. NOTE: stale image_cache rows from before this fix keep serving old labels/descriptions — clear them to see new behavior on re-upload. — 2026-08-24
- v0.3.6 fix — Gemini 404 on `gemini-2.5-flash`: Google retires models per-key-cohort; new API keys get 404 "no longer available to new users" even though the model catalog still lists it (verified live: catalog 200 + generateContent 404 with that exact body). Default switched to stable `gemini-3.6-flash` (live-verified 200; alternates 3.5-flash/3.5-flash-lite also verified). User's local `.env` updated too (env var overrides code default — the actual unblocker). Also added: Google's JSON error body is now logged before non-retryable HTTP errors propagate (the 404 reason was previously discarded, which made this hard to diagnose); regression tests for both behaviors. Graceful degradation worked as designed during the outage (201 Created, review flags, no upload blocked). Tests: 127 pass (+2), ruff clean. Commit `e559add` on `fix/v0.3.6-gemini-model-404`. — 2026-08-24
- v0.3.6 — Optional Gemini vision describer: `VISION_PROVIDER=groq|gemini` (default `groq`) selects ONLY the image-describer in the pipeline; qwen/Groq path untouched and scoring/text stages stay Groq-only. Gemini talks to Google's REST endpoint via pinned httpx — the official `google-genai` SDK was doc-verified then REJECTED at install (needs httpx>=0.28 vs supabase==2.9.0's httpx<0.28; venv restored to exact pins). Added provider-neutral `VisionRateLimitError` (subclasses `groq.RateLimitError`, builds a synthetic httpx.Response(429) because groq's APIStatusError reads `response.request`) so the pipeline circuit breaker trips identically for both providers. Env: `GEMINI_API_KEY`, `GEMINI_VISION_MODEL` (default `gemini-2.5-flash`). `.env.example`, `docs/setup.md`, ADR-0003 updated. Tests: 125 pass (+20 new), ruff clean. Commit `c4a17a1` on `feature/v0.3.6-gemini-vision-provider` — NOT merged/tagged; live API verification pending user's GEMINI_API_KEY. — 2026-08-24
- Unicode dash normalization fix — PDF/PPTX generators emit typographic dash variants (U+2010-U+2014, U+2212) that PyMuPDF/python-pptx extract verbatim and the LLM echoes into justifications. Added `normalize_unicode_dashes()` in the parsing agent (applied to all extracted PDF/PPTX text) and applied it to LLM justifications in the scoring agent. Also switched the scoring model from `llama-3.3-70b-versatile` (removed from Groq free tier, 404) to `openai/gpt-oss-120b` (roadmap alternative). Groq verified working end-to-end against a real submission. Tests: 41 total pass; ruff clean. Commits `869aaf7` (model fix) and `d5172bf` (dash normalization) on `feature/v0.3.0-scoring-agent`. — 2026-08-22
- v0.3.0 — Single scoring agent: Groq-powered, hardcoded 4-criteria rubric (problem_fit, technical_depth, feasibility, innovation), structured JSON score output with retry/backoff for rate limits and malformed JSON recovery. Adds `agents/scoring/scorer.py`, `infra/migrations/0003_create_scores.sql`, `POST /api/submissions/{id}/score` endpoint, `get_parsed_submission` and `insert_scores` Supabase service functions. Committed on `feature/v0.3.0-scoring-agent` (commit `51e7fef`), not yet merged to `main`. — 2026-08-22
- v0.2.0 — Parsing agent: extract structured text from PDF (PyMuPDF) and PPTX (python-pptx), chunk into sections, stored in a `parsed_submissions` table. Implemented and merged: committed directly on `main` as `ea558e7` (linear history — the earlier `feature/v0.2.0-parsing` note was inaccurate; no separate branch, no merge commit), followed by memory-bank docs commit `96fda45`. Adds `agents/parsing/extractor.py`, `infra/migrations/0002_create_parsed_submissions.sql`, `insert_parsed_submission` service, and parse-on-upload wiring in `POST /api/submissions`. Tests: 21 total (11 backend + 10 agents) pass; ruff clean. Local `main` in sync with `origin/main`. — 2026-08-21
- v0.1.0 — Project skeleton: Next.js upload portal, FastAPI backend with `/api/submissions` upload endpoint, Supabase Postgres + Storage wiring, SQL migration for `submissions` table, `.env.example` + `docs/setup.md` Supabase guide. Backend tests (8) + frontend tests (3) pass; ruff + eslint + prettier clean. — 2026-08-20
- CORS fix — Added `CORSMiddleware` to the FastAPI app (origins from `FRONTEND_URL` env var, default `http://localhost:3000`). Resolved the "Network error — could not reach the backend" issue in the upload portal. Merged into `main` (commit `6179319` + formatting fix `25537c4`). Backend tests now 10, all pass. — 2026-08-21

## In progress

- (none) — v1.1.0 shipped; starting v1.2.0 Notifications in a fresh session that reads this memory-bank first.
